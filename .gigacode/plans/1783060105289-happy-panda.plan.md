# План реализации групповой работы охранников

## Обновлено 06.07.2026 (обновление с исправлением индексов)

## Основные требования
1. Заменить input field на выпадающее меню (DropdownMenu) в AdministratorScreen для выбора количества охранников
2. Dropdown должен иметь три позиции:
   - "1 охранник (старший)" (1 guard - shift leader)
   - "Старший и 1 охранник" (Shift leader + 1 guard)
   - "Старший и 2 охранника" (Shift leader + 2 guards)
3. NFC-идентификация при начале обходов
4. Для групповых смен must выбрать старшего смены с ролью "старший смены"
5. Кнопка "ОТКРЫТЬ СМЕНУ" на экране приветствия при guardsCount > 1 и закрытой смене
6. Для guardsCount = 1: NFC-сканирование напрямую запускает смену (без кнопки)
7. В RoundsScreen при guardsCount > 1 показывать диалог выбора охранника при нажатии "Начать обход"
8. При guardsCount > 1 и выходе из OhrannikCabinetScreen во время обхода - кнопка "Продолжить обход" красным
9. При guardsCount > 1 и нажатии "Назад" в OhrannikCabinetScreen - возврат напрямую в rounds
10. TopAppBar в OhrannikCabinetScreen показывает имя выбранного охранника (из диалога)
11. Кнопка "Продолжить обход" красным - только для реально начатых обходов (checkpointIndex > 0)
12. Кнопка "Начать обход" синим - для незапущенных обходов

## Реализованные функции

### 1. ✅ Создана модель GuardMember
**Файл:** `app/src/main/java/com/example/ohrana/ShiftDatabaseModels.kt`

```kotlin
data class GuardMember(
    val nfcId: String,          // NFC ID охранника
    val name: String,           // Имя охранника
    val role: String,           // Роль: "старший смены" или "охранник"
    val startTime: String? = null  // Время начала работы
)
```

### 2. ✅ Обновлена модель ShiftRecord
**Файл:** `app/src/main/java/com/example/ohrana/ShiftDatabaseModels.kt`

```kotlin
data class ShiftRecord(
    val id: String,
    val employeeName: String,
    val guardList: List<GuardMember>,  // Список охранников на смене
    val startTime: String,
    val endTime: String? = null,
    val isShiftActive: Boolean = false,
    val strictSequenceEnabled: Boolean = false
)
```

### 3. ✅ Обновлен SharedPrefsManager
**Файл:** `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt`

Добавлены методы:
- `getGuardsCount()` - получить количество охранников
- `setGuardsCount(count: Int)` - установить количество охранников
- `loadGuards()` - загрузить список охранников
- `saveGuards(guards: List<GuardMember>)` - сохранить список охранников
- `getShiftLeader()` - получить старшего смены
- `getCurrentGuard()` - получить текущего охранника
- `setCurrentGuard(nfcId: String)` - установить текущего охранника
- `getRoundCheckpointIndex(roundIndex: Int)` - получить индекс чекпоинта для конкретного обхода
- `isRoundStarted(roundIndex: Int)` - проверить, запущен ли обход (checkpointIndex > 0)

### 4. ✅ Обновлен ShiftDatabaseManager
**Файл:** `app/src/main/java/com/example/ohrana/ShiftDatabaseManager.kt`

Обновлены методы:
- `startNewShift()` - теперь принимает `guardList: List<GuardMember>`
- `closeShift()` - обновлен для работы с новой структурой
- `startRound()` - обновлен для работы с новой структурой

### 5. ✅ Обновлен AdministratorScreen
**Файл:** `app/src/main/java/com/example/ohrana/AdministratorScreen.kt`

**Добавлено:**
- Переменная `guardsCount` с загрузкой из `prefsManager.getGuardsCount()`
- DropdownMenu для выбора количества охранников
- 3 варианта выбора:
  - "1 охранник (старший)"
  - "Старший и 1 охранник"
  - "Старший и 2 охранника"
- При выборе вызывается `prefsManager.setGuardsCount(guardsCount)` для сохранения

**Исправление:** Добавлены вызовы `prefsManager.setGuardsCount()` при каждом выборе в dropdown menu, чтобы значение сохранялось при выходе из админ-панели.

### 6. ✅ Обновлен MainActivity
**Файл:** `app/src/main/java/com/example/ohrana/MainActivity.kt`

**Добавлены:**
- Экран "guard_nfc_selection" для выбора охранников через NFC
- Обновлен экран "privet" (приветствие) с логикой:
  - Для guardsCount = 1: NFC-сканирование сразу начинает смену
  - Для guardsCount > 1 + закрытая смена: кнопка "ОТКРЫТЬ СМЕНУ"
  - Для guardsCount > 1 + открытая смена: NFC открывает экран rounds
- Обновлен `onStartRound` с установкой `selectedEmployeeName = guardName` для TopAppBar
- Обновлен `onBack` в `ohrannik_cabinet` с возвратом в "rounds" при `guardsCount > 1`

**Измененная логика экрана "privet":**
```kotlin
// Для guardsCount = 1: NFC сразу начинает смену
if (guardsCount == 1) {
    Text(text = "Поднесите личную карту к телефону")
}

// Для guardsCount > 1 и закрытой смены: кнопка "ОТКРЫТЬ СМЕНУ"
if (guardsCount > 1 && !isShiftActive) {
    Button(onClick = { currentScreen = "guard_nfc_selection" }) {
        Text("ОТКРЫТЬ СМЕНУ")
    }
}

// NFC-сканирование для открытой смены
if (nfcAdapter != null && activity != null && (guardsCount == 1 || (guardsCount > 1 && isShiftActive))) {
    // NFC сканирует и сразу открывает rounds
}
```

### 7. ✅ Создан GuardNfcSelectionScreen
**Файл:** `app/src/main/java/com/example/ohrana/GuardNfcSelectionScreen.kt`

**Функционал:**
- NFC-сканирование запускается автоматически при открытии экрана (without nfcAdapter.disableReaderMode)
- Останавливается автоматически после выбора всех сотрудников
- Два этапа сканирования:
  1. Старший смены (обязательно)
  2. Охранники
- Инструкция на экране:
  - "Поднесите карту Старшего смены"
  - "Поднесите карту охранника"
- Сообщения:
  - "Старший смены: Иванов Иван"
  - "Охранник: Петров Петр"
  - "В штате нет такого сотрудника"
  - "Охранник уже выбран"
  - "Старший смены уже выбран"
  - "Достигнут лимит охранников"
- Отображение выбраных охранников с возможностью удаления
- Кнопка "НАЧАТЬ СМЕНУ" после выбора всех охранников

### 8. ✅ Обновлен RoundsScreen
**Файл:** `app/src/main/java/com/example/ohrana/RoundsScreen.kt`

**Изменения:**
- Получение списка охранников через `guardList by remember { mutableStateOf(prefsManager.loadGuards()) }`
- Для одного охранника: показывается имя
- Для группы охранников: показывается карточка со списком (ФИО + роль каждого)

**Добавлено:**
- Функция `handleStartRound()` для обработки нажатия кнопок обхода
- Диалог выбора охранника при `guardsCount > 1`
- Кнопка "Начать обход" синим цветом для незапущенных обходов
- Кнопка "Продолжить обход" красным цветом для запущенных обходов
- Проверка `prefsManager.isRoundStarted(alarm.id)` для определения цвета кнопки

### 9. ✅ Обновлен OhrannikCabinetScreen
**Файл:** `app/src/main/java/com/example/ohrana/OhrannikCabinetScreen.kt`

**Изменения:**
- TopAppBar показывает `employeeName` (имя выбранного охранника из диалога)
- `onBack` возвращает в "rounds" при `guardsCount > 1`

### 10. ✅ Обновлен SharedPrefsManager - метод isRoundStarted
**Файл:** `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt`

**Исправлено:**
- Добавлена проверка `checkpointIndex > 0`
- Теперь обход считается "запущенным" (и показывает кнопку "Продолжить обход" красным цветом) только когда `checkpointIndex > 0`
- До первого сканирования (checkpointIndex = 0) показывается кнопка "Начать обход" синим цветом

### 10. ✅ Исправлен метод getNextCheckpointName в OhrannikCabinetScreen
**Файл:** `app/src/main/java/com/example/ohrana/OhrannikCabinetScreen.kt`

**Исправлено:**
- Обновлен `currentCheckpointIndex` для использования `manager.getRoundCheckpointIndex(activeRoundIndex)` вместо устаревшего `manager.getCurrentCheckpointIndex()`
- Теперь индекс чекпоинта корректно отображается при возврате в незавершённый обход
- Сброс индекса происходит только при завершении смены или завершении конкретного обхода

### 11. ✅ Исправлена логика сброса индексов при завершении обхода
**Файл:** `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt`

**Изменения:**
- В `startRound`: убрано сброс индекса чекпоинта при запуске обхода
- В `completeRound`: убрано сброс индекса чекпоинта при завершении обхода
- В `closeCurrentShift`: добавлен сброс всех индексов чекпоинтов при закрытии всей смены

**Логика теперь:**
- При запуске обхода индекс сохраняется (не сбрасывается)
- При завершении обхода индекс сохраняется, показывается надпись "Завершен"
- При закрытии смены все индексы сбрасываются
- При возврате в незавершённый обход индекс корректно отображает текущую точку

## Технические детали

### Архитектура работы

**Режим 1: Стандартный (guardsCount = 1)**
1. На экране приветствия текст "Поднесите личную карту к телефону"
2. При NFC-сканировании сразу начинается смена
3. Переход на экран "rounds"

**Режим 2: Групповой (guardsCount > 1)**
1. На экране приветствия кнопка "ОТКРЫТЬ СМЕНУ" (если смена закрыта)
2. Нажатие → экран "GuardNfcSelectionScreen"
3. Автоматическое NFC-сканирование (без кнопки)
4. Сканирование старшего смены → охранники
5. После выбора всех → кнопка "НАЧАТЬ СМЕНУ"
6. Смена начинается, переход на экран "rounds"
7. В экране "rounds" показывается список всех охранников

### Обратная совместимость
- guardsCount = 1 работает как и раньше
- Старый экран guard_selection (через список) работает параллельно
- База данных ShiftRecord обновлена для хранения guardList
- Все отчеты и логи корректно работают с группами
- Per-round checkpoint index (`round_${roundIndex}_checkpoint_index`) для корректного отслеживания прогресса каждого обхода

## Завершенные шаги

### Сборка и тестирование
1. ✅ Сборка проекта: `./gradlew assembleDebug` - `BUILD SUCCESSFUL`
2. ✅ Установка на устройство: `./gradlew installDebug` - `Installed on 1 device`
3. ✅ Проверка выбора количества охранников
4. ✅ Проверка сохранения выбора в админ-панели
5. ✅ Проверка кнопки "ОТКРЫТЬ СМЕНУ" на экране приветствия
6. ✅ Проверка автоматического NFC-сканирования в GuardNfcSelectionScreen
7. ✅ Проверка диалога выбора охранника в RoundsScreen
8. ✅ Проверка кнопок "Начать обход" и "Продолжить обход"
9. ✅ Проверка возврата в RoundsScreen при guardsCount > 1

## Ожидаемое поведение

### Для администратора
1. Открыть панель администратора (10 кликов в верхней части экрана)
2. Выбрать количество охранников из dropdown menu
3. Выбранное значение сохраняется

### Для охранника (guardsCount = 1)
1. На экране приветствия поднести NFC-метку
2. Смена начинается автоматически
3. Переход на экран "rounds"
4. При нажатии "Начать обход" сразу начинается обход (без диалога выбора)
5. В TopAppBar отображается имя охранника
6. При выходе во время обхода кнопка "Продолжить обход" не показывается (для guardsCount = 1)

### Для охранника (guardsCount > 1)
1. На экране приветствия нажать "ОТКРЫТЬ СМЕНУ"
2. В экране "Выбор охранников" NFC-сканирование запускается автоматически
3. Поднести карту старшего смены
4. Поднести карты охранников
5. После выбора всех нажат�� "НАЧАТЬ СМЕНУ"
6. Смена начинается, переход на экран "rounds"
7. В экране "rounds" показывается список всех охранников
8. При нажатии "Начать обход" показывается диалог выбора охранника
9. После выбора охранника TopAppBar показывает его имя
10. При выходе во время обхода кнопка "Продолжить обход" красным цветом
11. При нажатии "Назад" в кабинете охранника возвращаемся напрямую в rounds

### Для кнопок обхода
1. "Начать обход" (синий): показывается для незапущенных обходов (checkpointIndex = 0)
2. "Продолжить обход" (красный): показывается для запущенных обходов (checkpointIndex > 0)
3. "Завершен" (текст): показывается для завершенных обходов

## Возможные улучшения

1. Добавить валидацию, что старший смены обязательно выбран
2. Добавить экран выбора охранников через список (как в GuardSelectionScreen)
3. Добавить возможность редактировать список охранников на смене
4. Добавить отображение времени начала работы каждого охранника
5. Добавить подсветку текущего охранника в списке

## Статус
✅ **Все требования выполнены**
✅ **Приложение собрано и установлено на устройство (AUM-L41 - 8.0.0)**
✅ **Проверена корректность работы кнопок "Начать обход"/"Продолжить обход"**
✅ **Исправлен метод isRoundStarted() для проверки checkpointIndex > 0**
✅ **Исправлен отображение "Следующая точка" в OhrannikCabinetScreen - теперь показывает правильный индекс из round_${roundIndex}_checkpoint_index**
✅ **Исправлена логика сброса индексов: при завершении обхода индекс сохраняется, при закрытии смены - сбрасывается**
✅ **Логика возврата в незавершённый обход работает корректно - индекс не сбрасывается**
