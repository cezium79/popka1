# План: Система событий работников

## 1. Обзор задачи

Добавить возможность фиксации событий, связанных с работниками объекта (Staff), из экрана RoundsScreen. События сохраняются с привязкой к активной смене, отображаются в JournalScreen с фильтром по дате.

**Требования:**
- Кнопка "События работников" в bottomBar RoundsScreen
- Выбор работника из списка Staff
- 4 кнопки с готовыми шаблонами событий
- Поле для произвольного текстового события
- Сохранение с привязкой к shiftId, staff, timestamp
- Отображение в JournalScreen с фильтром по дате

---

## 2. Архитектурные решения

### Модель данных
- **StaffEvent** — новая модель события (аналогично IncidentRecord)
- Хранение в SharedPrefsManager как JSON-список (по аналогии с Staff)
- Ключ: `"staff_events_list"`

### UI паттерн
- Диалог на `Dialog` (как в StaffScreen) с прижатым к верху контентом
- Кнопки шаблонов в `Row` с `horizontalArrangement = Arrangement.spacedBy`
- Выпадающий список для выбора работника (`DropdownBox` или кастомный `AlertDialog`)
- Текстовое поле с `verticalScroll` при большом количестве событий

---

## 3. Детальный план реализации

### Шаг 1: Создание модели StaffEvent

**Файл:** `app/src/main/java/com/example/ohrana/StaffEvent.kt` (новый)

```kotlin
data class StaffEvent(
    val id: String = UUID.randomUUID().toString(),
    val timestamp: String,              // ISO формат времени
    val shiftId: String,                // ID активной смены
    val staffId: String,                // ID работника (Staff)
    val staffName: String,              // Имя работника (для отображения)
    val eventType: EventType,           // Тип события (шаблон или custom)
    val customText: String? = null,     // Произвольный текст (если eventType == CUSTOM)
    val templateText: String? = null    // Текст шаблона (для отображения)
)

enum class EventType(val ruName: String) {
    ARRIVED_AT_FACILITY("Пришел на объект"),
    LEFT_FACILITY("Ушел с объекта"),
    SMELL_OF_ALCOHOL("Пришел с запахом алкоголя"),
    MISSED_WORK("Не вышел на работу"),
    CUSTOM("Произвольный текст")
}
```

**Постоянные шаблоны:**
```kotlin
val PRESET_EVENTS = listOf(
    EventType.ARRIVED_AT_FACILITY,
    EventType.LEFT_FACILITY,
    EventType.SMELL_OF_ALCOHOL,
    EventType.MISSED_WORK
)
```

---

### Шаг 2: CRUD операции в SharedPrefsManager

**Файл:** `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt`

Добавить методы:

```kotlin
// Загрузка всех событий
fun loadStaffEvents(): List<StaffEvent> {
    val localPrefs = context.getSharedPreferences("OhranaPrefs", Context.MODE_PRIVATE)
    val jsonString = localPrefs.getString("staff_events_list", null)
    // Парсинг JSON -> List<StaffEvent>
}

// Сохранение всего списка
fun saveStaffEvents(events: List<StaffEvent>) {
    val localPrefs = context.getSharedPreferences("OhranaPrefs", Context.MODE_PRIVATE)
    val jsonArray = org.json.JSONArray()
    events.forEach { e ->
        val jsonObject = org.json.JSONObject()
        jsonObject.put("id", e.id)
        jsonObject.put("timestamp", e.timestamp)
        jsonObject.put("shiftId", e.shiftId)
        jsonObject.put("staffId", e.staffId)
        jsonObject.put("staffName", e.staffName)
        jsonObject.put("eventType", e.eventType.name)
        jsonObject.put("customText", e.customText ?: "")
        jsonObject.put("templateText", e.templateText ?: "")
        jsonArray.put(jsonObject)
    }
    localPrefs.edit().putString("staff_events_list", jsonArray.toString()).apply()
}

// Добавление одного события
fun addStaffEvent(event: StaffEvent) {
    val events = loadStaffEvents().toMutableList()
    events.add(event)
    saveStaffEvents(events)
}

// Фильтрация по дате и/или shiftId
fun getStaffEventsByDate(date: String, shiftId: String? = null): List<StaffEvent> {
    val events = loadStaffEvents()
    return events.filter {
        it.timestamp.startsWith(date) && 
        (shiftId == null || it.shiftId == shiftId)
    }
}
```

---

### Шаг 3: Диалог добавления события в RoundsScreen

**Файл:** `app/src/main/java/com/example/ohrana/RoundsScreen.kt`

**Изменения:**

1. **Добавить состояние диалога:**
```kotlin
var showEventDialog by remember { mutableStateOf(false) }
var selectedStaff by remember { mutableStateOf<Staff?>(null) }
var selectedEventType by remember { mutableStateOf<EventType?>(null) }
var customEventText by remember { mutableStateOf("") }
```

2. **Добавить кнопку в bottomBar** (перед кнопкой "ЗАВЕРШИТЬ СМЕНУ"):

```kotlin
OhranaOutlinedButton(
    text = "События",
    onClick = {
        selectedStaff = null
        selectedEventType = null
        customEventText = ""
        showEventDialog = true
    },
    modifier = Modifier.fillMaxWidth(),
    style = androidx.compose.ui.text.TextStyle(fontSize = 18.sp),
    designId = 3,
)
```

3. **Создать Dialog с:**
   - Выпадающим списком для выбора работника (используем `AlertDialog` со списком)
   - 4 кнопки шаблонов в `Row` / `Column`
   - Чекбокс/тоггл "Произвольный текст" для переключения на ручной ввод
   - `OutlinedTextField` для произвольного текста (показывается при тоггле)
   - Кнопки "Сохранить" и "Отмена"

**Структура диалога:**
```
┌─────────────────────────────────────┐
│ Добавление события                  │
├─────────────────────────────────────┤
│ Выбор работника: [▼ Выбрать]        │
│                                     │
│ Быстрые события:                    │
│ [Пришел на объект] [Ушел с объекта] │
│ [Пришел с запахом алк.] [Не вышел]  │
│                                     │
│ ☐ Произвольный текст                │
│ [Текст события...]                  │
│                                     │
│              [Отмена] [Сохранить]   │
└─────────────────────────────────────┘
```

4. **Логика сохранения:**
```kotlin
Button(onClick = {
    val shiftId = prefsManager.prefs.getString("active_shift_id", "") ?: "outside_shift"
    val timestamp = SimpleDateFormat("yyyy-MM-dd HH:mm:ss", Locale.getDefault()).format(Date())
    
    val eventType = if (selectedEventType != null) {
        selectedEventType!!
    } else {
        EventType.CUSTOM
    }
    
    val event = StaffEvent(
        timestamp = timestamp,
        shiftId = shiftId,
        staffId = selectedStaff!!.id,
        staffName = selectedStaff!!.fullName,
        eventType = eventType,
        customText = if (eventType == EventType.CUSTOM) customEventText else null,
        templateText = if (selectedEventType != null) selectedEventType!!.ruName else null
    )
    
    scope.launch {
        prefsManager.addStaffEvent(event)
        showEventDialog = false
    }
})
```

---

### Шаг 4: Отображение событий в JournalScreen

**Файл:** `app/src/main/java/com/example/ohrana/JournalScreen.kt`

**Изменения:**

1. **Добавить фильтр по дате** (если ещё нет)
2. **Загружать события работников:**
```kotlin
val staffEvents by remember { 
    mutableStateOf(prefsManager.getStaffEventsByDate(selectedDate, activeShiftId)) 
}
```

3. **Отобразить события в журнале** как карточки с иконкой `Icons.Default.Info` или кастомной иконкой:

```kotlin
staffEvents.forEach { event ->
    Card(
        modifier = Modifier.fillMaxWidth().padding(horizontal = 16.dp, vertical = 4.dp),
        colors = CardDefaults.cardColors(containerColor = Color(0xFF1976D2)) // Синий для событий
    ) {
        Column(modifier = Modifier.padding(12.dp)) {
            Row(horizontalArrangement = Arrangement.SpaceBetween) {
                Text("👤 ${event.staffName}", fontWeight = FontWeight.Bold)
                Text(event.timestamp, fontSize = 12.sp)
            }
            Spacer(modifier = Modifier.height(4.dp))
            Text(event.templateText ?: event.customText ?: "")
        }
    }
}
```

---

## 4. Список файлов для изменения

| Файл | Действие | Описание |
|------|----------|----------|
| `app/src/main/java/com/example/ohrana/StaffEvent.kt` | **Создать** | Модель данных + enum + константы |
| `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt` | **Изменить** | Добавить 4 метода для CRUD событий |
| `app/src/main/java/com/example/ohrana/RoundsScreen.kt` | **Изменить** | Добавить кнопку + Dialog + логику |
| `app/src/main/java/com/example/ohrana/JournalScreen.kt` | **Изменить** | Добавить отображение событий с фильтром |

---

## 5. Проверка

1. **Сборка:** `.\gradlew.bat assembleDebug` — без ошибок
2. **Добавление события:**
   - Открыть RoundsScreen при активной смене
   - Нажать "События работников"
   - Выбрать работника из списка
   - Нажать кнопку шаблона (или включить "Произвольный текст")
   - Нажать "Сохранить"
3. **Отображение в журнале:**
   - Перейти в JournalScreen
   - Выбрать дату события
   - Убедиться, что событие отображается в журнале
4. **Привязка к смене:** Убедиться, что события отображаются только за активную смену
