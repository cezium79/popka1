# План реализации: Система событий работников

## Реализация по плану `1785823655463-silent-falcon.plan.md`

---

## Шаг 1: Создание модели StaffEvent

**Файл:** `app/src/main/java/com/example/ohrana/StaffEvent.kt` (НОВЫЙ)

Создать файл с:
- `EventType` enum (ARRIVED_AT_FACILITY, LEFT_FACILITY, SMELL_OF_ALCOHOL, MISSED_WORK, CUSTOM)
- `StaffEvent` data class с полями: id, timestamp, shiftId, staffId, staffName, eventType, customText, templateText
- `PRESET_EVENTS` список из 4 шаблонов

---

## Шаг 2: CRUD операции в SharedPrefsManager

**Файл:** `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt`

Добавить 4 метода (по аналогии с существующими loadStaff/saveStaff):
- `loadStaffEvents(): List<StaffEvent>` — загрузка из key `"staff_events_list"`
- `saveStaffEvents(events: List<StaffEvent>)` — сохранение JSON
- `addStaffEvent(event: StaffEvent)` — добавить одно событие
- `getStaffEventsByDate(date: String, shiftId: String? = null): List<StaffEvent>` — фильтрация

---

## Шаг 3: Диалог добавления события в RoundsScreen

**Файл:** `app/src/main/java/com/example/ohrana/RoundsScreen.kt`

Изменения:
1. Добавить состояния: `showEventDialog`, `selectedStaff`, `selectedEventType`, `customEventText`
2. Добавить кнопку "События работников" в bottomBar (перед "ЗАВЕРШИТЬ СМЕНУ")
3. Создать AlertDialog с:
   - Списком работников для выбора (Dropdown)
   - 4 кнопками шаблонов в Row
   - Toggle для произвольного текста
   - OutlinedTextField для произвольного текста
   - Кнопки "Сохранить" и "Отмена"
4. Логика сохранения события

---

## Шаг 4: Отображение событий в JournalScreen

**Файл:** `app/src/main/java/com/example/ohrana/JournalScreen.kt`

Изменения:
1. Загружать события работников по выбранной дате
2. Отобразить события как карточки с синим фоном
3. Показать имя работника, время, тип/текст события

---

## Список файлов

| Файл | Действие |
|------|----------|
| `app/src/main/java/com/example/ohrana/StaffEvent.kt` | **Создать** |
| `app/src/main/java/com/example/ohrana/SharedPrefsManager.kt` | **Изменить** |
| `app/src/main/java/com/example/ohrana/RoundsScreen.kt` | **Изменить** |
| `app/src/main/java/com/example/ohrana/JournalScreen.kt` | **Изменить** |

---

## Проверка

1. `.\\gradlew.bat assembleDebug` — без ошибок
2. Тестирование UI: добавление события, отображение в журнале
