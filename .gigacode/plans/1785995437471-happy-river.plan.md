# План реализации ShiftFileStore

## Этап 1: Создать ShiftFileStore.kt
- Файл: `app/src/main/java/com/example/ohrana/ShiftFileStore.kt` (новый)
- Полный функционал: save, load, delete, list, cleanup

## Этап 2: Модифицировать SharedPrefsManager
- `closeCurrentShift()` — после генерации отчётов вызывать ShiftFileStore.saveShiftFile(), затем очищать SharedPreferences
- `startNewShift()` — убрать очистку staffEvents

## Этап 3: Модифицировать CloudStorageManager
- `generateHtmlReportWithDesign()` — добавить fallback на ShiftFileStore
- `uploadHtmlToDiskDirect()` — добавить fallback на ShiftFileStore

## Этап 4: Отключить архивную систему
- Закомментировать ArchiveService, ArchiveBroadcastReceiver, ArchiveManager
- Убрать регистрацию из AndroidManifest.xml
- Убрать кнопку архивации из AdministratorScreen.kt

## Этап 5: Проверка сборки
